# Data (too large for git)
data/raw/
data/processed/
experiments/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
*.so

# Virtual environments
.venv/
# server training venv (run/setup_env.sh)
.venv-linux/
# isolated ExecuTorch venv (run/setup_export_env.sh)
.venv-export/
venv/
env/
ENV/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Environment
.env
*.env

# Secrets / credentials (Kaggle API token, etc.)
.kaggle/
kaggle.json
.tmp/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# MLflow / W&B
mlruns/
wandb/

# Optuna
*.db

# Model files (large)
*.pth
*.pt
*.onnx
# ExecuTorch programs pulled from the training box (run/export_all_students.sh).
# NB: report_phase_1/.../efficientformerv2_s2.pte was committed before this rule
# and stays tracked — .gitignore does not retroactively untrack a file.
*.pte
exports/

# Fixed benchmark input set (run/make_benchmark_set.sh) — 100 images + their
# fully-preprocessed .bin tensors, ~116 MB per copy. Regenerate, don't commit.
data/benchmark_set/
data/benchmark_set_ef/

# Run transcripts (logs/<name>_<timestamp>.log)
logs/
*.out
*.err

# Claude Code — personal/local files (kept off git)
CLAUDE.local.md
.claude/settings.local.json
