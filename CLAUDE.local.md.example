# CLAUDE.local.md (template)

Copy this file to `CLAUDE.local.md` and edit. The real `CLAUDE.local.md` is
**gitignored** — it carries personal context (paths, machine specifics,
in-progress notes) that shouldn't be shared with other contributors.

```bash
cp CLAUDE.local.md.example CLAUDE.local.md
```

Both `CLAUDE.md` (project-wide) and `CLAUDE.local.md` (personal) are loaded
into Claude Code's context automatically.

---

## My environment

- **Local dev machine**: macOS (Apple Silicon) — no CUDA. Use `device: cpu` in configs when testing locally.
- **Real training server**: `<user>@<host>` (Linux + NVIDIA GPU, over SSH).
- **Project on the server**: `<abs path to the repo>`
- **venv on the server**: `<repo>/.venv-linux` (created by `run/setup_env.sh`)

## Personal preferences

- Always run `make lint` before committing.
- Launch long training runs under `tmux` on the server (`bash run/train_teacher.sh …`), never locally.
- When debugging training, check `experiments/<run>/training_curves.png` first.

## Active branches / tasks

- `feature/poc` — POC smoke-test pipeline (synthetic data + `run/poc.sh`)
- `feature/claude-config` — Claude Code skills + settings

## Datasets I have

- ISIC 2024 — downloaded? Y / N — location: `data/raw/isic2024/`
- PAD-UFES-20 — downloaded? Y / N
- HAM10000 — for cross-domain eval only — downloaded? Y / N
- Fitzpatrick17k — for fairness eval only — downloaded? Y / N

## Notes for Claude

(Personal scratchpad — anything you want to remind yourself or Claude about
this project.)
